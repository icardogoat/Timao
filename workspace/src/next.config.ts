// This file is intentionally left empty to avoid conflicts with the root next.config.ts.
// Next.js expects a single next.config.ts file in the project root.
const nextConfig = {};

export default nextConfig;
