'use client';

// This component is no longer in use as the dedicated login page has been removed.
// All login actions are now handled by the LoginButton on the homepage.
export function LoginButtons() {
  return null;
}
